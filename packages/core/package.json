{
  "name": "@showdown-mobile/core",
  "version": "0.0.0",
  "private": true,
  "description": "Platform-agnostic Pokémon Showdown connection, auth and session layer",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pkmn/login": "^0.2.0",
    "@pkmn/protocol": "^0.7.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.11"
  }
}
